# 本番環境の環境変数テンプレート
# コピーして実際の値を設定してください:
#   cp .env.production.example .env.production

# Cloudflare Worker の URL
# wrangler deploy 後に表示される URL を設定する
VITE_WORKER_URL=https://YOUR_WORKER_NAME.YOUR_SUBDOMAIN.workers.dev
